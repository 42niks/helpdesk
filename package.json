{
  "name": "apartment-helpdesk",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply helpdesk-db"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

